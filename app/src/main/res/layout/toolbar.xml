<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="@dimen/toolbar_height"
    android:layout_marginLeft="@dimen/toolbar_main_margin_left"
    android:layout_marginRight="@dimen/toolbar_main_margin_left"
    android:layout_marginTop="@dimen/toolbar_main_margin_top"
    android:background="@drawable/background_bolder_toolbar"
    android:elevation="3dp"
    app:layout_scrollFlags="scroll|enterAlways"
    app:popupTheme="@style/ThemeOverlay.AppCompat.Light"
    app:theme="@style/ThemeOverlay.AppCompat.Light">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/title"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:drawableLeft="@drawable/ic_launcher_background"
            android:drawablePadding="10dp"
            android:gravity="left|center_vertical"
            android:maxLines="1"
            android:text="Search"
            android:textAllCaps="false"
            android:textColor="@color/colorAccent"
            android:textSize="@dimen/text_size_normal"/>

    </LinearLayout>

</android.support.v7.widget.Toolbar>